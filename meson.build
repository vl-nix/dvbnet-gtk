project('dvbnet-gtk', 'c', version: '1.1.1', meson_version: '>= 0.50.0',  default_options: [ 'warning_level=2', 'c_std=gnu11'])

bindir = join_paths(get_option('prefix'), get_option('bindir'))

subdir('src')
subdir('data')
